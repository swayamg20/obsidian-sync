just completed with creating a bash pipeline to automatic sync my `quartz-portfolio` with my `obsidian-sync`.
damn it was nightmare, setting up launchd is tough!
the macOS native notification wasn't working well initially, had to install `terminal-notifier` 
used [[deployment]] to test.